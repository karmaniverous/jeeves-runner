/**
 * Runner configuration schema and types.
 *
 * Delegates to {@link @karmaniverous/jeeves-runner-core} for the canonical
 * schema definition. Re-exported here for internal consumers.
 *
 * @module
 */

export { type RunnerConfig, runnerConfigSchema } from '@karmaniverous/jeeves-runner-core';
