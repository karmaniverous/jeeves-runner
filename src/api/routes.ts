/**
 * API route handlers.
 *
 * @module
 */

import type { FastifyInstance } from 'fastify';

export const registerRoutes = (_app: FastifyInstance): Promise<void> => {
  throw new Error('Not implemented');
};
